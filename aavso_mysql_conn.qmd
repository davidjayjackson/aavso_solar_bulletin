---
title: "Sunspots + MySQL"
format: html
editor: visual
---

## Quarto

```{r, message=FALSE,echo=FALSE}
library(DBI)
library(RMariaDB)
library(dbplyr) # Run R code against database
library(tidyverse)
```

## Connect to DB

```{r}

con <- dbConnect(
  RMariaDB::MariaDB(),
  dbname = "solar",
  host="localhost",
#  host = "alnilam.aavso.org",
  user = "root",
  password = "dJj135790",
  port = 3306
)
```

## List Databases On Server

```{r}
dbGetQuery(con, "SHOW DATABASES;")
```


## List Tables In Database

```{r}
dbListTables(con)
```

## Lost Columns in Table

```{r}
dbListFields(con, "sun_header")
```
